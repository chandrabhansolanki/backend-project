{
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^6.7.2",
    "script": "^0.1.4"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "nodemon server.js"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
